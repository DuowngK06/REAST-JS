import {configureStore} from "@reduxjs/toolkit"
import studen_slient from "./slices/studenSlice"
const storeRedux = confixgureStone({
    reducer{
        students:student_slice
    }
})
export default storeRedex;
